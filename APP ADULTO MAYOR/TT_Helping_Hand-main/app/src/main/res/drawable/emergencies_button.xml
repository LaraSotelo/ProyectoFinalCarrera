<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color de fondo del botón -->
    <solid android:color="@color/emergency_buttons" />

    <!-- Definición del borde del botón -->
    <stroke
        android:width="2dp"
        android:color="@color/emergency_buttons" />

    <!-- Hacer el botón redondo -->
    <corners android:radius="25dp" />

    <!-- Margen interno (opcional) -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
