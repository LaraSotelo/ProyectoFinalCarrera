<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Definición de bordes -->
    <stroke
        android:width="2dp"
        android:color="@color/edges"/>

    <!-- Definición de esquinas redondeadas -->
    <corners android:radius="15dp"/> <!-- Ajusta el radio para redondear más o menos las esquinas -->

    <!-- Opcional: Margen interno del EditText -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp"/>
</shape>
